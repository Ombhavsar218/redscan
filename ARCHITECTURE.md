# 🏗️ RedScan AI - Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                       │
├─────────────────────────────────────────────────────────────┤
│  Web Browser                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Dashboard   │  │ Scan Details │  │ Admin Panel  │      │
│  │  (index)     │  │ (scan_detail)│  │ (Django)     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      DJANGO WEB LAYER                        │
├─────────────────────────────────────────────────────────────┤
│  URLs (rescanai/urls.py)                                    │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  /              → index (dashboard)                   │  │
│  │  /scan/start/   → start_scan (API)                    │  │
│  │  /scan/<id>/    → scan_detail (results)               │  │
│  └──────────────────────────────────────────────────────┘  │
│                            │                                 │
│  Views (rescanai/views.py)                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  • index()         - Display dashboard                │  │
│  │  • start_scan()    - Trigger new scan (API)           │  │
│  │  • scan_detail()   - Show scan results                │  │
│  │  • execute_scan()  - Background scan execution        │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    SCANNING ENGINE LAYER                     │
├─────────────────────────────────────────────────────────────┤
│  scanner.py                                                  │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  NetworkScanner                                     │    │
│  │  ├─ scan_port()      - Test single port            │    │
│  │  ├─ scan_ports()     - Parallel port scanning      │    │
│  │  ├─ banner_grab()    - Service version detection   │    │
│  │  └─ resolve_hostname() - DNS resolution            │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  VulnerabilityScanner                               │    │
│  │  ├─ check_common_vulnerabilities()                 │    │
│  │  └─ calculate_risk_score()                         │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  ReconEngine                                        │    │
│  │  ├─ whois_lookup()                                 │    │
│  │  └─ subdomain_enum()                               │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      DATABASE LAYER                          │
├─────────────────────────────────────────────────────────────┤
│  Models (rescanai/models.py)                                │
│                                                              │
│  ┌──────────────┐      ┌──────────────┐                    │
│  │   Target     │      │     Scan     │                    │
│  ├──────────────┤      ├──────────────┤                    │
│  │ name         │◄─────│ target_id    │                    │
│  │ target_type  │      │ scan_type    │                    │
│  │ target_value │      │ status       │                    │
│  │ created_by   │      │ started_at   │                    │
│  └──────────────┘      │ completed_at │                    │
│                        └──────────────┘                    │
│                               │                              │
│                    ┌──────────┴──────────┐                 │
│                    ▼                      ▼                 │
│         ┌──────────────┐      ┌──────────────────┐        │
│         │     Port     │      │  Vulnerability   │        │
│         ├──────────────┤      ├──────────────────┤        │
│         │ scan_id      │      │ scan_id          │        │
│         │ port_number  │      │ port_id          │        │
│         │ protocol     │      │ title            │        │
│         │ service      │      │ description      │        │
│         │ version      │      │ severity         │        │
│         │ state        │      │ cvss_score       │        │
│         └──────────────┘      │ cve_id           │        │
│                               │ remediation      │        │
│                               └──────────────────┘        │
│                                                              │
│         ┌──────────────┐                                    │
│         │   ScanLog    │                                    │
│         ├──────────────┤                                    │
│         │ scan_id      │                                    │
│         │ timestamp    │                                    │
│         │ level        │                                    │
│         │ message      │                                    │
│         └──────────────┘                                    │
│                                                              │
│  SQLite Database (db.sqlite3)                               │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow: Scan Execution

```
1. USER ACTION
   │
   ├─► User clicks "Start Scan" or calls API
   │
   ▼
2. API REQUEST
   │
   ├─► POST /scan/start/ with target_id
   │
   ▼
3. VIEW PROCESSING (start_scan)
   │
   ├─► Create Scan record in database
   ├─► Set status = 'pending'
   ├─► Launch background thread
   │
   ▼
4. BACKGROUND THREAD (execute_scan)
   │
   ├─► Update status = 'running'
   ├─► Log: "Starting scan..."
   │
   ▼
5. NETWORK SCANNING (NetworkScanner)
   │
   ├─► Resolve hostname to IP
   ├─► Scan ports (1-1024) in parallel
   │   └─► ThreadPoolExecutor with 100 workers
   │       └─► Each worker: socket.connect_ex()
   │
   ├─► For each open port:
   │   ├─► Identify service (socket.getservbyport)
   │   ├─► Grab banner (send/recv)
   │   └─► Save Port record to database
   │
   ▼
6. VULNERABILITY SCANNING (VulnerabilityScanner)
   │
   ├─► Check dangerous ports (21, 23, 445, etc.)
   ├─► Check for misconfigurations
   ├─► For each finding:
   │   └─► Save Vulnerability record to database
   │
   ├─► Calculate risk score (0-10)
   │   └─► Weight by severity: Critical=10, High=7, etc.
   │
   ▼
7. COMPLETION
   │
   ├─► Update status = 'completed'
   ├─► Set completed_at timestamp
   ├─► Log: "Scan completed. Risk score: X.XX/10"
   │
   ▼
8. USER VIEWS RESULTS
   │
   └─► Dashboard shows scan status
       └─► Click scan → Detailed results page
           ├─► Open ports table
           ├─► Vulnerabilities table
           ├─► Risk breakdown
           └─► Scan logs
```

## Component Interactions

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │ HTTP Request
       ▼
┌─────────────┐
│  Django     │
│  Views      │◄──────┐
└──────┬──────┘       │
       │              │ Query/Save
       │              │
       ▼              │
┌─────────────┐       │
│  Scanner    │       │
│  Engine     │       │
└──────┬──────┘       │
       │              │
       │ Network      │
       │ Requests     │
       ▼              │
┌─────────────┐       │
│   Target    │       │
│   System    │       │
└─────────────┘       │
       │              │
       │ Results      │
       └──────────────┘
                      │
                      ▼
              ┌─────────────┐
              │  Database   │
              │  (SQLite)   │
              └─────────────┘
```

## Threading Model

```
Main Django Thread
│
├─► Request Handler (start_scan)
│   │
│   ├─► Create Scan record
│   │
│   └─► Spawn Background Thread ──────┐
│                                      │
└─► Return Response (scan_id)         │
                                       │
                                       ▼
                            Background Thread
                            │
                            ├─► execute_scan()
                            │   │
                            │   ├─► NetworkScanner
                            │   │   │
                            │   │   └─► ThreadPoolExecutor
                            │   │       ├─► Worker 1 (port 1)
                            │   │       ├─► Worker 2 (port 2)
                            │   │       ├─► Worker 3 (port 3)
                            │   │       └─► ... (up to 100)
                            │   │
                            │   ├─► VulnerabilityScanner
                            │   │
                            │   └─► Save results to DB
                            │
                            └─► Thread exits
```

## Security Layers

```
┌─────────────────────────────────────────┐
│  1. Input Validation                    │
│     • Target format validation          │
│     • Port range limits                 │
│     • Rate limiting (future)            │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  2. Authentication (future)             │
│     • User login required               │
│     • API token validation              │
│     • Permission checks                 │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  3. Network Safety                      │
│     • Timeout limits                    │
│     • Connection pooling                │
│     • Error handling                    │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  4. Data Storage                        │
│     • SQL injection protection (ORM)    │
│     • Encrypted passwords               │
│     • Audit logging                     │
└─────────────────────────────────────────┘
```

## File Structure

```
redscan/
│
├── manage.py                    # Django management script
├── requirements.txt             # Python dependencies
├── README.md                    # Project overview
├── LEARNING_GUIDE.md           # Educational content
├── COMMANDS.md                 # Quick reference
├── ARCHITECTURE.md             # This file
├── setup.py                    # Setup helper script
├── test_scanner.py             # Standalone scanner test
│
├── redscan/                    # Main project config
│   ├── __init__.py
│   ├── settings.py             # Django settings
│   ├── urls.py                 # Root URL routing
│   ├── wsgi.py                 # WSGI config
│   └── asgi.py                 # ASGI config
│
└── rescanai/                   # Main application
    ├── __init__.py
    ├── models.py               # Database models
    ├── views.py                # Request handlers
    ├── urls.py                 # App URL routing
    ├── admin.py                # Admin interface config
    ├── scanner.py              # Core scanning engine
    ├── apps.py                 # App configuration
    ├── tests.py                # Unit tests
    │
    ├── migrations/             # Database migrations
    │   └── __init__.py
    │
    └── templates/              # HTML templates
        └── rescanai/
            ├── base.html       # Base template
            ├── dashboard.html  # Main dashboard
            └── scan_detail.html # Scan results
```

## Technology Stack

```
┌─────────────────────────────────────────┐
│  Frontend                               │
│  • HTML5                                │
│  • CSS3 (embedded)                      │
│  • JavaScript (future: AJAX updates)    │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  Backend Framework                      │
│  • Django 4.2+                          │
│  • Python 3.8+                          │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  Core Libraries                         │
│  • socket (network I/O)                 │
│  • threading (concurrency)              │
│  • concurrent.futures (thread pool)     │
│  • subprocess (external tools)          │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│  Database                               │
│  • SQLite (development)                 │
│  • PostgreSQL (production ready)        │
└─────────────────────────────────────────┘
```

## Scalability Considerations

### Current (Single Server)
```
Browser → Django → Scanner → Target
                ↓
            SQLite DB
```

### Future (Distributed)
```
                    ┌─► Scanner Node 1
Browser → Django ───┼─► Scanner Node 2
            ↓       └─► Scanner Node 3
        PostgreSQL
            ↓
        Redis Queue (Celery)
```

## Performance Metrics

```
Current Performance:
├─► Port Scan (1-1024): ~10-30 seconds
├─► Concurrent Threads: 100
├─► Timeout per port: 1 second
└─► Database: SQLite (suitable for <1000 scans)

Optimization Opportunities:
├─► Use Nmap (10x faster)
├─► Implement caching
├─► Add result pagination
├─► Use Celery for background tasks
└─► Switch to PostgreSQL for scale
```

## Extension Points

Where to add new features:

```
1. New Scan Types
   └─► scanner.py: Add new scanner class

2. New Vulnerability Checks
   └─► scanner.py: VulnerabilityScanner.check_common_vulnerabilities()

3. New Data Fields
   └─► models.py: Add fields to models
   └─► Run: python manage.py makemigrations

4. New API Endpoints
   └─► views.py: Add new view function
   └─► urls.py: Add URL pattern

5. New UI Pages
   └─► templates/: Add new HTML template
   └─► views.py: Add view to render it

6. External Tool Integration
   └─► scanner.py: Add wrapper class
   └─► Example: NmapScanner, MetasploitScanner
```

---

This architecture is designed to be:
- **Modular**: Easy to add new scanners
- **Extensible**: Clear extension points
- **Educational**: Learn by modifying
- **Scalable**: Can grow with your needs

Happy building! 🏗️
